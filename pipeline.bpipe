load "minc-library.bpipe"

run {
 "%.mnc" * [clean_and_center + anlm_denoise + n4correct + linear_bestlinreg + cutneckapplyautocrop + nonlinear_register + beastnormalize + beastmask + beastextract + classify_clean + extractvbm + resamplevbm]
}
