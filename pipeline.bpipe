load "minc-library.bpipe"

run {
 "%.mnc" * [n4correct + linear_register + nonlinear_register + cutneckapplyautocrop + beastnormalize + beastmask + beastextract + classify_clean + extractvbm + resamplevbm]
}

